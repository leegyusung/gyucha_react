let jwt;_409‍.x([["generateAccessToken",()=>generateAccessToken],["verifyToken",()=>verifyToken]]);_409‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);require("dotenv").config();

const { ACCESS_TOKEN_SECRET } = process.env;


       const generateAccessToken = _id => {
    return jwt.sign({ _id }, ACCESS_TOKEN_SECRET, {
        expiresIn: '15m'
    })
}

       const verifyToken = (req, res, next) => {
    try {
        const clientToken = req.cookies.user;
        const decoded = jwt.verify(clientToken, ACCESS_TOKEN_SECRET);
        _409‍.g.console.log(decoded);
        if (decoded) {
            res.locals.user = decoded._id;
            next();
        } else {
            res.status(401).json({ error: 'unauthorized' });
        }
    } catch (error) {
        res.status(401).json({ error: 'token expired' });
    }
}